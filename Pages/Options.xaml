<UserControl x:Class="Flarial.Pages.Options"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Flarial.Pages"
             xmlns:controls="clr-namespace:Flarial.Controls"
             mc:Ignorable="d" 
             d:DesignHeight="275" d:DesignWidth="525">
    <Border CornerRadius="15,15,0,0" Background="#211917" ClipToBounds="True">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="160"/>
                <ColumnDefinition/>
            </Grid.ColumnDefinitions>
            <!-- Sidebar -->
            <Border Margin="0" Background="#1c1513" CornerRadius="15,15,0,0">
                <StackPanel Margin="10">
                    <Label Content="Options" Style="{DynamicResource Heading}" Margin="0,0,0,20"/>
                    <controls:SidebarButton IsActive="True" Text="General" Icon=" " x:Name="GeneralBtn" Click="SidebarButton_Click"/>
                    <controls:SidebarButton Text="Versions" Icon=" " x:Name="VersionsBtn" Click="SidebarButton_Click"/>
                    <controls:SidebarButton Text="Appearance" Icon=" " x:Name="AppearanceBtn" Click="SidebarButton_Click"/>
                    <controls:SidebarButton Text="About" Icon=" " x:Name="AboutBtn" Click="SidebarButton_Click"/>
                </StackPanel>
            </Border>
            <!-- Content -->
            <ContentControl Grid.Column="1" x:Name="ContentArea" Margin="5"/>
        </Grid>
    </Border>
</UserControl>
